# 🔬 牛顿环实验测量工具 — 项目交接文档

> 交接日期：2026-08-26
> 项目路径：`newton-ring/`（Git 仓库，远端 `git@github.com:picktsh/newton-ring.git`，分支 `master`）

---

## 一、项目概述

本项目是一个**纯前端（浏览器端）的牛顿环自动测量工具**，专为大学物理实验设计：

- 上传牛顿环实验照片后，自动检测**圆心**和**暗环**；
- 自动计算各暗环直径，并用逐差法公式 **R = (Dm² − Dn²) / [4(m−n)λ]** 计算透镜曲率半径；
- 内置**像素标定值测定**功能（通过测微鼓轮两次拍照对比，算出 mm/像素）；
- 全部计算在浏览器内完成，无后端、无数据库、无需安装依赖。

**技术栈**：Vue 3（`js/lib/vue.global.js`）+ OpenCV.js（`js/lib/opencv.js`，约 9.7MB，异步加载）+ ES6 原生模块。无构建工具，无 npm 依赖。

---

## 二、运行与部署

| 项 | 说明 |
|---|---|
| 本地运行 | 直接双击 `index.html`（需现代浏览器，Chrome 80+/Firefox 75+/Edge 80+，支持 ES6 模块） |
| 说明文档页 | `readme.html` 通过 `fetch('README.md')` + marked.js 动态渲染（⚠️ `file://` 协议下 fetch 会被 CORS 拦截，需起本地 HTTP 服务，如 `npx http-server`） |
| 线上部署 | `.github/workflows/static.yml`：push 到 `master` 自动部署到 GitHub Pages（**上传整个仓库**，`img/` 等素材文件也会被发布） |
| 首次加载 | 需等待 OpenCV.js（9.7MB）下载并初始化完成（日志面板会提示"OpenCV 已就绪"），未就绪时无法处理图片 |

---

## 三、功能清单

页面顶部有两个 Tab（状态持久化在 `sessionStorage['activeTab']`）：

### Tab 1：🔬 牛顿环识别（主功能）

1. **图片上传与管理**
   - 点击上传区或**拖拽**上传（拖拽仅在牛顿环识别 Tab 生效）；支持多选批量上传；支持 JPG / PNG / WEBP。
   - 顶部缩略图列表：点击切换、× 删除；切换时若该图已有结果则直接恢复缓存，否则自动触发检测。
   - 批量上传时**只自动处理最后一张**，其余图片在切换到时才处理。
2. **自动检测流程**（上传后自动执行，也可点"重新处理"重跑）
   - 预处理：灰度化 → CLAHE(1.5, 16) 对比度增强 → 中值滤波去噪；
   - 圆心定位：霍夫圆检测外边界 → 失败时退回亮度加权质心 → 再用"圆周亮度标准差最小"在 ±15px 局部网格精修；
   - 暗环检测：72 角度径向剖面采样（双线性插值）→ Savitzky-Golay 平滑 → 局部最小值查找（对比度阈值 15）→ 非极大值抑制 → 半径聚类（容差 = 预期间距 40%，至少 20% 角度出现）→ **r²∝m 线性拟合精修**（偏差 >15% 才修正）；
   - 结果按半径升序编号 1..N，提取四关键点并做椭圆拟合。
   - 每个阶段会把中间图像显示在"识别结果"区（调试可视化）。
3. **参数调试面板**（8 个滑杆）：亮度、对比度、模糊、灰度、锐化、边缘增强、CLAHE Clip、CLAHE Tile。调整时有**实时 CSS 滤镜预览**，点"重新处理"后用 OpenCV 重新检测。
4. **像素标定输入**：数字框手动输入 `mm/像素`（默认 0.005，**不持久化，刷新即重置**）。
5. **可视化交互**
   - Canvas 叠加绘制检测圆环、编号标签、圆心十字；
   - 鼠标悬停圆环 ↔ 表格行**双向联动高亮**。
6. **结果展示**
   - 表1：各暗环直径（像素 + mm）；
   - 表2：曲率半径分组计算（**动态分组，m−n 固定差 3**）；
   - 平均曲率半径卡片。
7. **数据导出**
   - 导出 CSV：含 BOM（Excel 直接打开不乱码），包含直径表 + 曲率半径表 + 元信息；
   - 导出识别截图：底图 + Canvas 标注层合并为 PNG。
8. **日志面板**：实时显示处理进度（[10%]…[100%]）与状态提示，最多保留 20 条。

### Tab 2：📏 像素标定值测定

目的：实验测微鼓轮移动已知距离，换算出真实的"mm/像素"，替代默认标定值。

1. 分别上传**图A、图B**（转动测微鼓轮前后拍的同一视野）；
2. 在两张图上**点击同一物理特征点**（绘制精细十字标记 + 坐标标注）；
3. **方向键微调**标记点（1px/次，Shift+方向键 5px/次）；
4. 输入两张图对应的鼓轮刻度（mm），系统自动算出刻度差作为物理距离，也可手动覆盖；
5. 自动计算：ΔX、ΔY、像素距离、**标定值 = 物理距离 / 像素距离**；
6. 点"✅ 应用此标定值到牛顿环识别"→ 写入主流程的 `pixelScale` 并自动切回牛顿环 Tab；
7. 标定图片、标记点、刻度全部自动缓存到 sessionStorage，刷新不丢。

---

## 四、代码架构（`js/` 目录）

| 文件 | 职责 |
|---|---|
| `main.js` | 应用入口，挂载 Vue 组件 |
| `app.js` | **主组件**：全部状态管理（681 行）、两 Tab 流程编排、标定功能全部逻辑、CSV/PNG 导出 |
| `image-manager.js` | 图片上传、指纹去重、切换、删除、检测结果缓存（sessionStorage 封装） |
| `image-processor.js` | **核心算法**：预处理、霍夫圆心检测、径向剖面暗环检测、物理聚类拟合、椭圆拟合（760 行） |
| `canvas-drawer.js` | Canvas 绘制检测圆环、编号、十字标记 |
| `data-calculator.js` | 直径/曲率半径计算、动态分组、平均半径 |
| `interaction-handler.js` | Canvas 悬停交互、表格行联动、拖拽上传 |
| `constants.js` | `LAMBDA = 589.3e-9`（钠光波长）、暗环标记颜色 |

调用链：`app.js processImage()` → `image-processor.processNewtonRings()` → 检测结果存 `image-manager` 缓存 → `canvas-drawer.drawDetectionResults()` 绘制 → `data-calculator` 计算表格数据。

### sessionStorage 缓存键一览

| Key | 内容 |
|---|---|
| `newtonRing_images` | 所有上传图片（base64 全文！） |
| `calc_<指纹>` | 对应图片的检测结果（暗环数据） |
| `lastViewedFingerprint` | 上次查看的图片 |
| `activeTab` | 当前 Tab |
| `calib_images` / `calib_points` | 标定图片与标记点 |

图片指纹 = `文件名_文件大小_最后修改时间`。

---

## 五、⚠️ 注意事项与已知坑（重点阅读）

### 算法相关

1. **环编号是假设性的**：检测到的暗环按半径升序编号 1..N，**隐含假设最内圈就是第 1 级暗环**。若中心被遮挡、出现伪环或漏检，编号整体偏移，曲率半径会产生系统性误差。交接后如需提高精度，应增加"起始环级数"人工确认环节。
2. **"约 22 环"的硬编码假设**：`findMinimaDirect` 与 `clusterRadiiByPhysics` 中平均环间距 = `maxRadius / 22`，由此推导对比度检查窗口、抑制间距、聚类容差。若图像环数显著多于/少于 22，检测阈值需相应调整。
3. **硬编码阈值**：最小对比度 `minContrast = 15`、聚类出现率 20%（降级 12%）、r² 拟合修正触发偏差 15%、圆心精修范围 ±15px。均在 `image-processor.js` 内，无 UI 暴露。
4. **分组逻辑与 README 不符**：`data-calculator.js` 实际是**动态分组、m−n 固定差 3、无组数上限**（"最多 5 组"的代码已被注释掉）。20 个环会产生 17 组。README 里写的"5 组固定分组 (20-15…)"是旧描述。

### 数据与缓存相关

5. **sessionStorage 容量约 5MB**：图片以 base64 全文存储，上传多张大图会**静默保存失败**（仅 `console.warn`），表现为刷新后缓存丢失。建议图片宽度控制在 2000px 内。
6. **指纹碰撞风险**：同名、同大小、同修改时间的不同图片会误命中旧缓存；反之重命名/重新保存同一张图会被当作新图重新处理。
7. **`pixelScale` 不持久化**：默认 0.005，刷新页面后重置，需从标定 Tab 重新应用（或手动输入）。
8. 点"重新处理"会先删除当前图的 `calc_` 缓存再重跑；缓存恢复时 `Int32Array` 轮廓已做序列化兼容（转普通数组）。

### 交互相关

9. **方向键监听是全局的**（`document` 级 `keydown`）：只要标定标记点存在，**任何 Tab 下**按方向键都会移动标记点，并且 `preventDefault` 会拦截数字输入框的原生方向键行为。
10. 拖拽上传**仅在牛顿环识别 Tab** 生效（`initDragDrop` 传入了 `activeTab === 'rings'` 判断）。
11. 滤镜面板参数调整时的预览是 **CSS filter**，"重新处理"才走 OpenCV 真实管线；其中 CLAHE 参数会传入 OpenCV 处理，其余参数对实际检测的作用有限，调参后务必观察检测结果而非预览。

### 部署相关

12. GitHub Pages workflow **上传整个仓库**（`path: '.'`），`img/` 里的实验照片素材也会公开发布——如含敏感素材需先清理或修改 workflow。
13. `readme.html` 在 `file://` 协议下无法加载 README.md（CORS），本地查看说明页需起 HTTP 服务；线上部署不受影响。
14. 根目录的 `__test-module.mjs` 是开发期测试脚本，非产品代码。

### 物理参数

- 波长 λ = 589.3 nm（钠光），定义在 `constants.js`，若换光源需改代码；
- 直径公式中 mm → m 的单位换算（×1e-6）写死在 `data-calculator.js`。

### 数据修约规则（有效数字 / 小数位）

本项目对各物理量的显示做了分类修约，接手时需注意不同列遵循不同规则：

1. **逐差法表2「平方差 D_m² − D_n² (mm²)」**（按误差传播定小数位）
   - 该列本质是减法，遵循「加减法按小数点位数对齐；中间计算多保留 1 位，最终结果再修约」。
   - 平方项小数位由误差传播决定：直径 D 显示/精确到 0.001 mm（末位 1 个单位），其平方的绝对精度 ≈ `2·D·0.001`；将该精度修约到 **1 位有效数字**，其末位所在小数位即为平方项应保留的小数位。
   - 减法对齐取 `D_m²`、`D_n²` 中**较少**的小数位（即较大直径对应的位数）。
   - 中间量 `diffSquared` 保留全精度（多于「多保留 1 位」），最终显示用**四舍六入五成双**（复用 `roundHalfEven`）修约到该小数位。
   - 举例：D ≈ 9.6 mm → 2 位小数（如 `26.31`）；D ≈ 2.5 mm → 3 位小数（如 `3.898`）。
   - 代码位置：`data-calculator.js` 的 `squareDiffDecimals(Dm, Dn)`，结果字段 `diffSqDecimals` / `diffSquaredText`；`index.html` 表2 与 CSV 导出均使用 `diffSquaredText`。
2. **逐差法表2「曲率半径 R (m)」**（每组有效数字由本组差值决定）
   - 逐差法**每组独立**计算 R；本组 R 的有效数字 = **本组差值 `D_m²−D_n²` 的有效数字**（按已修约显示值数，即 `countSigFigs(diffSquared, diffSqDecimals)`）。
   - **不被 λ 的 4 位封顶**：差值 5 位 → R 也 5 位（如 `1.5714`）；减法会损失有效数字，**各组位数可不同**。
   - R 由**全精度 `diffSquared`** 算出（中间全程多保留），每组完成减法后**只对本组 R 修约一次**，用**四舍六入五成双**（`formatSigFigsHalfEven`），显示为**普通小数**（不用科学计数）。
   - **下游一致性**：平均曲率半径 R̄ 与不确定度统计（A 类、`u_B(R)_i`）均使用**各组修约后的 R**（`radiusRounded`），做到「拿计算器按表2 显示的 R 求平均」逐位吻合。
   - 无效组（差值 ≤ 0 / 缺环）：R 显示 `—`，不参与统计。
   - 代码位置：`data-calculator.js` 的 `countSigFigs` / `formatSigFigsHalfEven`，结果字段 `radiusSigFigs` / `radiusRounded` / `radiusText`（`radius` 仍存全精度供内部参考）；`index.html` 表2 与 CSV 导出均使用 `radiusText`。
3. **像素标定值（mm/像素）**
   - 统一保留 **4 位有效数字**（按有效数字而非小数位修约）。
   - 代码位置：`app.js` 的 `calibValueSigFigs` 固定返回 4，显示用 `formatSigFigs`（表2 每行标定值、大字结果、应用到 Tab2 全链路一致）。
4. **保持不变的部分**（勿误改）
   - 表1 直径：仍按原始需求稿固定 **3 位小数**（`.toFixed(3)`）。
   - 平均曲率半径大字 R̄ 与 `meanText`：仍由**不确定度对齐**决定小数位（`roundHalfEven(R̄, U 的小数位)`），不套用「每组 R 有效数字」规则；无不确定度时回退 `averageRadius.toFixed(3)`。
   - 不确定度：沿用原有规则（`sigU` 只进不舍、首位有效数字为 1 或 2 时保留 2 位）。

---

## 六、常见问题速查

| 现象 | 处理 |
|---|---|
| 检测不到暗环 | 图片质量差或中心遮挡；调高对比度/CLAHE 后重新处理 |
| 环数不对 | 调整亮度/模糊/锐化；检查图片分辨率（建议 >800px）；必要时改 `minContrast` 阈值 |
| 圆心偏移 | 确认中心光斑未被遮挡；调整亮度对比度后重试 |
| 刷新后数据丢失 | 多半是 sessionStorage 超限静默失败，减少图片数量/尺寸 |
| 加载慢 | OpenCV.js 首次加载 5-10 秒属正常；大图处理 2-5 秒 |
| 计算结果系统性偏差 | 检查环编号起点是否正确（见注意事项 1）、像素标定值是否真实 |

---

## 七、交接清单

- [ ] 确认接手人已能双击 `index.html` 跑通全流程（上传 → 检测 → 导出）
- [ ] 知晓分组逻辑、环编号假设、22 环硬编码等算法前提（第五节 1-4）
- [ ] 知晓 `pixelScale` 不持久化与 sessionStorage 容量限制（第五节 5-7）
- [ ] 检查 `img/` 目录素材是否需要从 GitHub Pages 发布中排除
- [ ] README 中"5 组固定分组"描述已过时，可在下次维护时同步更新

---

*文档基于 2026-08-26 的 master 分支代码（`app.js` v 681 行 / `image-processor.js` v 760 行）梳理。*
